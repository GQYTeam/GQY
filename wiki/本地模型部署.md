# 本地模型部署

GQY 通过 **OpenAI 兼容协议** 接入本地推理服务，支持 llama.cpp 与 Ollama 两种后端。推荐 **llama.cpp**（更轻、更快、可跟随菜单栏启停）。

## 推荐：llama.cpp

```zsh
brew install llama.cpp
mkdir -p ~/llama-models
# 把 GGUF 模型放进来，例如 qwen3-abliterated-8b-q4.gguf（去审查）：
# 可从 Ollama 缓存复用：ln -sf ~/.ollama/models/blobs/<sha256> ~/llama-models/qwen3-abliterated-8b-q4.gguf
```

### 关闭思考（Qwen3 系）

Qwen3 默认开启 thinking，会让本地 8B 模型慢 10 倍以上。用自定义 no-think 模板：

```
# ~/llama-models/qwen3-nothink.jinja
{%- if messages[0]['role'] == 'system' %}<|im_start|>system
{{ messages[0]['content'] }}<|im_end|>
{%- endif %}
{%- for message in messages %}
{%- if message['role'] == 'user' %}<|im_start|>user
{%- if loop.last %}{{ message['content'] }} /no_think{%- else %}{{ message['content'] }}{%- endif %}<|im_end|>
{%- elif message['role'] == 'assistant' %}
{%- if message['content'] %}<|im_start|>assistant
{{ message['content'] }}<|im_end|>
{%- endif %}
{%- elif message['role'] == 'tool' %}<|im_start|>user
<tool_response>
{{ message['content'] }}
</tool_response><|im_end|>
{%- endif %}
{%- endfor %}
<|im_start|>assistant
```

启动：

```zsh
llama-server -m ~/llama-models/qwen3-abliterated-8b-q4.gguf \
  --host 127.0.0.1 --port 8080 -c 8192 \
  --alias qwen3-abl-nothink \
  --chat-template-file ~/llama-models/qwen3-nothink.jinja
```

### 跟随菜单栏启停

菜单栏 App 启动时自动拉起 llama-server（127.0.0.1:8080，已有服务则复用），退出时一起关闭；默认**不开机自启**。只有勾选菜单栏「开机自启」时才注册 LaunchAgent 随登录启动。

## 备选：Ollama

```zsh
brew install ollama && brew services start ollama
ollama pull qwen3:8b
# 去审查 + 关思考副本：
printf 'FROM huihui_ai/qwen3-abliterated:8b\nPARAMETER enable_thinking false\n' > /tmp/Modfile
ollama create qwen3-abl-nothink -f /tmp/Modfile
```

## 接入 GQY

```zsh
# 自动发现模型 + 激活默认
gqy provider add http://127.0.0.1:8080/v1 --api-key local --id llamacpp --name "llama.cpp 本地"
# 或
gqy provider add http://127.0.0.1:11434/v1 --api-key local --id ollama --name "Ollama 本地"

gqy models          # 查看/切换
```

## 速度参考（M2 16GB）

| 模型 | 配置 | 首回复 |
|---|---|---|
| qwen3-abliterated-8b（关思考）| llama.cpp | ~2s |
| qwen3-abliterated-8b（关思考）| Ollama no-think 模板 | ~2.5s |
| qwen3-abliterated-8b（默认 thinking）| 任意 | 1-2 分钟（不推荐）|

## 注意事项

- 8B 模型面对 GQY 全量工具集（30 个）容易陷入工具调用循环：**请在闲聊模式使用本地模型**（轻量工具集），或 CLI 用 `--plan` 只读模式
- 模型输出的 `<think>` 空块由 GQY 流式层自动剥除（0.8.3+），无需担心
