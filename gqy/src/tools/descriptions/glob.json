{
  "name": "glob",
  "display_name": "查找文件",
  "description": "查找文件。",
  "parameters": {
    "type": "object",
    "properties": {
      "path": {
        "type": "string",
        "description": "搜索目录，默认工作区；查用户文件用 ~ 或 /home，受保护的全局搜索可用 /。"
      },
      "pattern": {
        "type": "string",
        "description": "glob 模式。"
      },
      "max_results": {
        "type": "integer",
        "description": "最多结果数。"
      }
    },
    "required": [
      "pattern"
    ]
  },
  "permission": "readonly",
  "always_loaded": true,
  "load_policy": "summary",
  "groups": [
    "dev-read"
  ]
}
