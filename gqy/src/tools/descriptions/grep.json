{
  "name": "grep",
  "display_name": "搜索内容",
  "description": "用 ripgrep 在目录或文件中搜索内容。",
  "parameters": {
    "type": "object",
    "properties": {
      "path": {
        "type": "string",
        "description": "目录或文件，默认工作区。"
      },
      "pattern": {
        "type": "string",
        "description": "正则模式。"
      },
      "include": {
        "type": "string",
        "description": "可选大小写不敏感文件 glob 过滤。"
      },
      "max_results": {
        "type": "integer",
        "description": "最多匹配数。"
      }
    },
    "required": [
      "pattern"
    ]
  },
  "permission": "readonly",
  "always_loaded": true,
  "load_policy": "summary",
  "groups": [
    "dev-read"
  ]
}
